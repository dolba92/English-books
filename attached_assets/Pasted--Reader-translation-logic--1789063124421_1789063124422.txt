Сейчас НЕ делай полный редизайн приложения и НЕ меняй Reader/translation logic.

Выполни ТОЛЬКО этот этап и остановись после его завершения.

КРИТИЧЕСКИ ВАЖНО:
НЕ изменяй никакой код, связанный с:
- переводом слов;
- переводом предложений;
- грамматическим анализом;
- translation providers/API;
- word popup translation logic;
- sentence translation logic.

Эти функции сейчас работают быстро и правильно. Они должны остаться полностью рабочими.

ЗАДАЧА ЭТОГО ЭТАПА:

1. Полностью удалить раздел "Тренировка" из пользовательского приложения:
- удалить пункт "Тренировка" из sidebar;
- удалить страницу тренировок;
- удалить карточки тренировочных режимов;
- удалить "Повторение ошибок";
- убрать ссылки и кнопки, ведущие на Training.

2. Полностью убрать SRS / интервальное повторение из интерфейса:
- удалить блок "Интервальное повторение" из Настроек;
- удалить связанные с ним пользовательские настройки;
- удалить тренировочную статистику из Progress, если она там есть.

При удалении не трогать shared-код, если от него зависит Reader, перевод или сохранённые слова.

3. Переименовать раздел "Словарь" в:

Слова

Он должен быть только списком слов, сохранённых пользователем во время чтения.

4. На странице "Слова" добавить:
- checkbox у каждого слова;
- "Выбрать все";
- количество выбранных;
- "Удалить выбранные";
- "Экспорт TXT".

5. Экспортировать ТОЛЬКО выбранные слова, если есть selection.

Если ничего не выбрано, предложить экспортировать все слова.

TXT формат строго:

English word — русский перевод

Одна запись на строку.

Например:

inside — внутри
talking — говорить
burning — горящий

UTF-8.

Никаких CSV, JSON, Excel, Word или PDF.

6. Сохранить существующие:
- поиск слов;
- сортировку;
- добавление слова из Reader;
- удаление слов.

7. После удаления Training обновить sidebar:

Библиотека
Слова
Прогресс
Настройки

Никакого пустого места под удалённым пунктом.

8. НЕ меняй на этом этапе:
- Library design;
- книжную полку;
- обложки;
- Reader layout;
- pagination;
- fonts;
- themes;
- translation UI;
- grammar UI;
- mobile layout.

Это будут отдельные следующие этапы.

9. Проверь после изменений:
- книга открывается;
- слово нажимается;
- перевод слова работает как раньше;
- предложение переводится как раньше;
- вкладка "Грамматика" работает как раньше;
- слово добавляется в "Слова";
- TXT реально скачивается и содержит выбранные слова.

После выполнения ОСТАНОВИСЬ.
Не начинай самостоятельно следующий этап.